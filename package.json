{
  "name": "wechat-claude-code",
  "version": "1.0.0",
  "description": "Control Claude Code remotely via WeChat — with permission approval, push notifications, and interrupt control",
  "type": "module",
  "bin": {
    "wechat-claude-code": "dist/cli.js",
    "wechat-claude-code-server": "dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "engines": { "node": ">=22" },
  "license": "MIT",
  "files": [
    "dist",
    "hooks"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
